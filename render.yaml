databases:
  - name: maple-exchange-db
    plan: starter
    databaseName: maple_exchange
    user: maple

services:
  - type: redis
    name: maple-exchange-redis
    plan: starter
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []

  - type: web
    name: maple-exchange-api
    runtime: node
    plan: starter
    repo: https://github.com/moespamemail21-dotcom/maple-exchange-api
    branch: main
    buildCommand: npm install && npm run build && npm run db:push
    startCommand: npm run start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: maple-exchange-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: maple-exchange-redis
          type: redis
          property: connectionString
      - key: JWT_SECRET
        sync: false
      - key: WALLET_ENCRYPTION_KEY
        sync: false
      - key: WALLET_SEED_BTC
        sync: false
      - key: WALLET_SEED_ETH
        sync: false
      - key: WALLET_SEED_LTC
        sync: false
      - key: WALLET_SEED_XRP
        sync: false
      - key: WALLET_SEED_SOL
        sync: false
      - key: STORAGE_BACKEND
        value: local
